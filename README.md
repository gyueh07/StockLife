# StockLife v3.5 Login Hard Fix

GitHub Pages 배포용 완성본입니다.

## 포함 기능
- Firebase 로그인 / 회원가입 / 로그아웃
- 이름 + 비밀번호 로그인 방식
- Firestore 자동 저장
- 총 자산 랭킹 TOP 20
- 홈 / 시장 / 종목 상세 / 지갑 / 랭킹 / 프로필
- 시장 검색 / 정렬 / 상승·하락·보유 필터
- 관심 종목 및 시장 상단 고정
- 5분 / 1시간 / 1일 / 1주 / 전체 차트
- 차트 십자선
- 매수/매도 MAX, 수량 직접 입력
- 부족 금액/부족 수량 경고 및 버튼 비활성화
- 거래 확인창
- 평균단가, 평가손익, 수익률
- 최근 거래 내역 20개
- Min/Max 보정 + 종목별 변동성 가격 알고리즘
- 숫자 애니메이션, 로딩, 토스트, 모바일 최적화

## GitHub Pages
루트에 index.html, css 폴더, js 폴더를 업로드하세요.


## v3.1 변경점
- 로그인/회원가입을 이메일이 아니라 이름으로 변경했습니다.
- Firebase Auth는 내부적으로 이메일 형식이 필요해서, 입력한 이름을 숨겨진 내부 이메일로 변환합니다.
- 예: `철수` → 내부 계정 이메일 형식으로 자동 변환
- 사용자는 이메일을 볼 필요가 없습니다.

## 중요
Firebase Authentication에서 Email/Password 로그인이 켜져 있어야 합니다.
GitHub Pages 주소가 Firebase Authentication > Settings > Authorized domains에 등록되어 있어야 합니다.


## v3.2 변경점
- CSS 캐시 방지: `style.css?v=3.2`
- JS 캐시 방지: `app.js?v=3.2`
- 이름 로그인 내부 도메인을 더 안전한 형식으로 변경
- 로그인 성공 후 화면 전환 중 오류가 나면 토스트로 표시
- 구버전 v3.1 계정 로그인 fallback 추가


## v3.4 변경점
- 모든 종목 상장일을 `2026.07.09 00:10`으로 고정했습니다.
- 전체 차트는 상장일 기준으로 시작합니다.
- 마우스 이동 시 실제 주식 차트처럼 세로선 + 가로선 + 점 + 날짜/시간 + 가격이 보이도록 수정했습니다.
- 상장 전 구간은 억지 데이터로 만들지 않습니다.


## v3.5 변경점
- 로그인/회원가입 핸들러를 완전히 단순화했습니다.
- 이름을 안전한 내부 이메일로 변환합니다.
- 로그인 성공 즉시 홈 화면으로 이동합니다.
- 로그인 실패 이유를 화면에 표시합니다.
- 캐시 방지 버전을 `?v=3.5`로 올렸습니다.

## 이번 수정
- 차트 기록을 `localStorage`의 `stocklife.chartHistory.v1` 키에 저장해 같은 브라우저와 같은 사이트 주소에서 다음 업데이트 후에도 기존 가격 기록을 유지합니다.
- 상장 시간을 `2026.07.09 00:10`으로 변경하고 시장 화면에도 표시합니다.

## v3.6 변경점
- 매수/매도 완료 시 화면 상단에 거래 알림이 표시됩니다.
- 누락되어 있던 사용자 저장 함수를 복구해 거래 후 현금, 보유 종목, 총자산, 랭킹 데이터가 Firestore에 저장됩니다.
- 랭킹 화면을 열 때 현재 총자산을 먼저 저장한 뒤 TOP 20을 다시 조회합니다.
- CSS/JS 캐시 방지 버전을 `?v=3.6`으로 올렸습니다.

## v3.7 변경점
- 거래 확인 버튼 중복 실행을 막아 빠른 클릭이나 타이밍 문제로 매수/매도가 여러 번 반영되는 문제를 줄였습니다.
- 사용자 데이터 정규화를 추가해 예전 저장값이나 문자열 숫자가 섞여도 현금, 보유 수량, 총자산 계산이 안정적으로 처리됩니다.
- 저장 요청을 순서대로 처리해 오래된 저장이 최신 거래 결과를 덮어쓰지 않게 했습니다.
- 랭킹 갱신 시 불필요한 중복 저장을 줄이고, 랭킹 화면 진입 시에만 현재 총자산을 먼저 동기화합니다.
- 앱 프레임의 `overflow:hidden`을 제거해 지갑의 거래 기록처럼 긴 화면을 아래로 스크롤할 수 있게 했습니다.
- CSS/JS 캐시 방지 버전을 `?v=3.7`로 올렸습니다.

## v3.8 변경점
- 전량 매도 후 새로고침하면 현금은 늘고 보유 주식이 다시 살아나던 문제를 수정했습니다.
- 거래 저장 시 Firestore 문서를 최신 사용자 상태로 통째로 저장해, 삭제된 보유종목 키가 서버에서도 확실히 삭제됩니다.
- CSS/JS 캐시 방지 버전을 `?v=3.8`로 올렸습니다.

## v3.9 변경점
- 매수/매도 처리를 Firestore 트랜잭션으로 변경했습니다.
- 같은 계정을 두 탭이나 두 기기에서 동시에 거래해도 서버의 최신 현금/보유수량을 기준으로 다시 검사합니다.
- 거래 저장이 성공한 뒤에만 화면의 현금, 보유종목, 거래내역을 갱신합니다.
- CSS/JS 캐시 방지 버전을 `?v=3.9`로 올렸습니다.
